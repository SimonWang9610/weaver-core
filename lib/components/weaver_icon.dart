import 'package:flutter/material.dart';

class WeaverIcons {
  static const chatOutline = Icon(Icons.chat_outlined);
  static const peopleOutline = Icon(Icons.people_outline);
  static const eventNoteOutline = Icon(Icons.event_note_outlined);
  static const walletGiftcard = Icon(Icons.wallet_giftcard);
  static const settings = Icon(Icons.settings);
  static const accountCircleOutline = Icon(Icons.account_circle_outlined);
  static const accountBalanceOutline = Icon(Icons.account_balance_outlined);
  static const couldUpload = Icon(Icons.cloud_upload);
  static const languageOutline = Icon(Icons.language_outlined);
  static const lock = Icon(Icons.lock);
  static const lockOutline = Icon(Icons.lock_outline);
  static const deleteForever = Icon(Icons.delete_forever);
  static const logout = Icon(Icons.logout);
  static const support = Icon(Icons.support);
  static const infoOutline = Icon(Icons.info_outline);
}
